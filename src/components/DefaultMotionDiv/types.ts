import { ReactNode } from 'react';

interface DefaultMotionDivProps {
    divKey: string;
    children: ReactNode;
    fullWidth?: boolean;
    fullHeight?: boolean;
}

export type { DefaultMotionDivProps };
